import 'package:flutter/material.dart';

class MobileSplacePage extends StatelessWidget {
  const MobileSplacePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Splace Screen"),
      ),
    );
  }
}
