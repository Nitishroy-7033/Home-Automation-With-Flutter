import 'package:flutter/material.dart';

class WebRoomPage extends StatelessWidget {
  const WebRoomPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}