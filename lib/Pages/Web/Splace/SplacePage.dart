import 'package:flutter/material.dart';

class WebSplacePage extends StatelessWidget {
  const WebSplacePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Splace Screen"),
      ),
    );
  }
}
